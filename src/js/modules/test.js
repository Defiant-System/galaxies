
let Test = {
	init(APP) {
		
		// setTimeout(() => window.find(`.toolbar-tool[data-arg="5"]`).trigger("click"), 500);
		// setTimeout(() => APP.dispatch({ type: "new-game" }), 500);
		// setTimeout(() => APP.dispatch({ type: "pause-game" }), 1500);
		// setTimeout(() => APP.dispatch({ type: "open-help" }), 100);

	}
};
